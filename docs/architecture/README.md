# Architecture

Durable technical decisions and system-level design documentation for Iroko —
the "what and why" that doesn't change with every audit finding. Distinct
from `docs/exec-plans/` (bounded, time-boxed implementation plans) and from
`docs/local/` (working notes, drafts, superseded audits).

Nothing here yet — this directory exists so the category declared in
`docs/index.md`'s directory policy is real, not just promised. Add documents
here as durable architectural decisions get written up (multi-tenancy model,
RLS strategy, billing provider abstraction, etc.).
