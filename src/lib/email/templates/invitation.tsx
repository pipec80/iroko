// Stub — full implementation in Task 2.
import type React from 'react';

/** Props for the team invitation email template. */
export type InvitationEmailProps = {
  inviterEmail: string;
  role: string;
  inviteUrl: string;
};

/** Invitation email sent when a user is invited to join a team. */
export function InvitationEmail(_props: InvitationEmailProps): React.ReactElement | null {
  return null;
}
