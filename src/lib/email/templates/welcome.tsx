// Stub — full implementation in Task 2.
import type React from 'react';

/** Props for the welcome email template. */
export type WelcomeEmailProps = {
  firstName: string;
};

/** Welcome email sent after the user's first login. */
export function WelcomeEmail(_props: WelcomeEmailProps): React.ReactElement | null {
  return null;
}
